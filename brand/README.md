# Brand Assets

Place your brand logo files here:

- `logo.png` — Main logo (used in Navbar, About section, Footer)
- `favicon.png` — Browser tab icon (32×32 or 64×64 recommended)
- `og-image.png` — Social share image (1200×630 recommended)

The website will show a text fallback automatically if `logo.png` is missing.
